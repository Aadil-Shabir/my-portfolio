"use client";

interface AccordionContentWrapperProps {
    children: React.ReactNode;
}

const AccordionContentWrapper = ({ children }: AccordionContentWrapperProps) => {
    return <>{children}</>;
};

export default AccordionContentWrapper;
